# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# 运行时生成
comfyui_multi/
output/
outputs/
respect_zip/
*.log

# 临时文件
*.tmp
_test_*.py

# 密钥 / 本地配置（千万不要提交）
.env
.env.*
*.key
secrets.*
config.local.*

# 独立仓库：剧本→AI视频自动化生产台（自带 git，单独推送）
# 见 github.com/NKZDDD/script-to-video-studio
script-to-video-studio/

# skill 是上面那个仓库的内容，这里只是目录联接（junction）
.claude/skills/script-to-video-prompts/
.claude/skills/script-to-video-prompts-v2/

# 生产项目产物（体积大，按需另行备份）
projects/

# 会话导出的聊天记录（体积大）
*剧本自动化流程优化.html
*剧本自动化流程优化_files/
*Shared Grok Conversation.html
*Shared Grok Conversation_files/

# 系统
.DS_Store
Thumbs.db
desktop.ini

# 编辑器
.vscode/
.idea/
*.swp
*~

# ---- 参考资料，不入库 ----------------------------------------------------
# skill 原稿（docx）和解包出来的目录：是**参考**，不是本仓库的源码。
# 提交它们的坏处：几百 KB 的二进制每改一版就多一份历史，而且 skill 有自己
# 的版本节奏，混进插件仓库之后没人说得清哪一版对应哪一版代码。
*.docx
*.doc
produce-cinematic-ai-short-drama*/

# 各家网关的接口文档（apifox 导出、飞书导出、厂商给的 md/txt）。
# 里面常带**示例 key 和账号信息**，而且是别人的资料，不该进公开仓库。
*接口文档*
*API文档*
*apifox*
*Documentation.md

# 发布包与构建快照。releases/ 424 MB、v61_all_providers_build_* 311 MB，
# 都是 script-to-video-studio 打出来的 exe 和整份代码拷贝 ——
# 那个项目有自己的仓库，要发版走 Release，别塞进插件仓库的历史。
releases/
*_build_2*/
*.zip
